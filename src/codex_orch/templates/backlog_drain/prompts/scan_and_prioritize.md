Scan the codebase for issues. Produce a prioritized backlog as `result.json` with:
- `backlog`: ordered array of items, each with `id`, `title`, `severity`, and `description`
- `total_found`: count of issues discovered
- `summary`: one-paragraph overview
